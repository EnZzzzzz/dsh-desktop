{
  "name": "dsh-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell for the official DeepSeek Harness Web UI: spawns `dsh web` locally and renders it in Electron",
  "type": "module",
  "main": "out/main/index.js",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "start": "electron-vite preview",
    "typecheck": "tsc --noEmit",
    "package": "node scripts/prepare-node.mjs && node scripts/prepare-runtime.mjs && electron-vite build && electron-builder",
    "package:dir": "node scripts/prepare-node.mjs && node scripts/prepare-runtime.mjs && electron-vite build && electron-builder --dir"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.0-rc.7"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "electron": "^38.0.0",
    "electron-builder": "^25.1.8",
    "electron-vite": "^4.0.0",
    "typescript": "^5.9.0"
  },
  "allowScripts": {
    "@deepseek-ai/dsh-subprocess-local@0.1.0-rc.7": true,
    "electron@38.8.6": true,
    "esbuild@0.25.12": true,
    "esbuild@0.28.2": true,
    "koffi@3.1.5": true,
    "node-pty@1.2.0-beta.15": true,
    "@google/genai": false,
    "protobufjs": false
  }
}
